# Python
venv/
.venv/
env/
__pycache__/
*.pyc
*.pyo
*.pyd

# Cache de parquets ONS (versionado via path — ver decisão 5.34)
.cache/

# Smoke test parquets do térmico (Fases A/A.1/A.2 — desenvolvimento, ~30 MB)
_eneva_legado/_smoke/

# Pytest
.pytest_cache/

# Output regenerável do scripts/validar_casamento_excel_ons.py
relatorio_casamento/

# Previews descartáveis dos scripts de geração de logos
assets/logos/_preview_*.png

# Config com credenciais reais — JAMAIS commitar
config.yaml
# Backups manuais antes de mexer no config (ex: config.yaml.bak.20260518)
config.yaml.bak.*

# Streamlit secrets (se existirem no futuro)
.streamlit/secrets.toml
# Service Account JSONs do Google Cloud (gspread / Sheets API) —
# arquivo de credencial baixado do GCP. JAMAIS commitar.
.streamlit/*.json

# Claude Code (settings pessoais)
.claude/settings.local.json

# SO
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/

# Cache disk dos loaders SQL (mmgd_sql etc) — regenerável via primeira execução
cache/

# Premissas por usuário da aba Receita por Empresa — estado de runtime
# (placeholders + inputs do usuário), não versionado.
data/premissas_receita_modulacao.json
